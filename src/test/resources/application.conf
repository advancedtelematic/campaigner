akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "DEBUG"
}

database = {
  catalog = "campaigner_test"
}

deviceRegistry.uri = "localhost"
director.uri = "localhost"

scheduler = {
  delay = 10 milliseconds
  batchSize = 10
}
